{{- if .Values.ingress.admin.enabled }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "sternsoft.fullname" . }}-admin
  annotations:
    {{- if (eq .Values.config.clusterProvider "aks") }}
    kubernetes.io/ingress.class: addon-http-application-routing
    {{- else }}
    kubernetes.io/ingress.class: nginx
    {{- end }}
    {{- if .Values.ingress.admin.tls }}
    {{- if (empty .Values.ingress.admin.certificate) }}
    cert-manager.io/cluster-issuer: {{ .Values.ingress.admin.issuer.name }}
    kubernetes.io/tls-acme: 'true'
    {{- end }}
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    {{- end }}
  labels:
    app: {{ template "sternsoft.name" . }}-admin
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  rules:
    - host: {{ .Values.ingress.admin.hostname }}
      http:
        paths:
          - path: /
            backend:
              serviceName: {{ template "sternsoft.fullname" . }}-admin
              servicePort: http
  {{- if .Values.ingress.admin.tls }}
  tls:
    - secretName: {{ template "sternsoft.admin-certificate" . }}
      hosts:
        - {{ .Values.ingress.admin.hostname }}
  {{- end }}
{{- end }}
