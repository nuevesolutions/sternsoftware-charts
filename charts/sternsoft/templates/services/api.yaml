{{- if .Values.config.api.internal }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "sternsoft.fullname" . }}-api
  labels:
    app: {{ template "sternsoft.name" . }}-api
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  {{- if .Values.ingress.api.enabled }}
  type: ClusterIP
  {{- else }}
  type: {{ .Values.service.api.type }}
  {{- if (or (eq .Values.service.api.type "LoadBalancer") (eq .Values.service.api.type "NodePort")) }}
  externalTrafficPolicy: {{ .Values.service.api.externalTrafficPolicy }}
  {{- end }}
  {{- end }}
  ports:
    - name: http
      port: {{ (and (eq .Values.service.api.type "LoadBalancer") (not (empty .Values.service.api.ports.http))) | ternary .Values.service.api.ports.http "80" }}
      targetPort: container
      {{- if (and (eq .Values.service.api.type "NodePort") (not (empty .Values.service.api.ports.http))) }}
      nodePort: {{ .Values.service.api.ports.http }}
      {{- end }}
  selector:
    app: {{ template "sternsoft.name" . }}-api
    release: {{ .Release.Name }}
{{- end}}